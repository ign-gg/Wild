name: Wild
version: "${pom.version}"
api: ["1.0.0"]
author: PetteriM1
main: tk.suomicraftpe.commands.WildCommand

commands:
  wild:
    description: Teleport to a random location
    usage: "/wild"
    aliases: ["rtp"]
    permission: commands.wild

permissions:
  commands.wild:
    description: "Allow usage of /wild"
    default: true
